//
//  Game3ViewController.swift
//  Ceria
//
//  Created by Kathleen Febiola Susanto on 10/10/22.
//

import UIKit

class Game3ViewController: UIViewController {
    
    //Game 3 detail (Kath was here)
    
    //Garuda HP -> 5000

    //Tallu HP -> 1000

    //Normal Hit -> 500

    //Critical Hit -> 1000

    //Garuda One Hit -> 200

    //Beginning -> load Garuda idle animation

    //Garuda attack every 10 second (Randomly to left or right) -> load Garuda attack animation

    //Arrow - Swipe/Drag Gesture Recognizer -> show line on where the arrow will lend based on current swipe, on release throw Arrow (animation follow the line)

    //Dodging Garuda attack - accelerometer Y value if -, to the left, if + to the right, load wheel turning animation


    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
